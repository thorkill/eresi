
How to build elfsh, e2dbg, libasm and libelfsh :


1. ./configure --help (select your options)
2. make
3. make install (optional, you can test from e2dbg/e2dbg and vm/elfsh)

BETATESTERS: If you want to make install this tarball, be sure to edit 
vm/e2dbg (its a script) and uncomment the 2 lines that you should find 
easily for LD_PRELOAD path.

4. cd testsuite && make (optional)


Read each directory README file for more information.